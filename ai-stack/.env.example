# Copy to .env (same directory) and paste your key from
# https://openrouter.ai/settings/keys  — create the key WITH a credit limit.
# .env is gitignored; never commit the real key.
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
